### 01｜
```C
#include <stdio.h>
#include <string.h>
char line[100];
int main()
{
	int n;
	scanf("%d\n\n", &n);
	
	for(int i=0; i<n; i++)
	{
		while(gets(line)!=NULL)		//讀取失敗
		{
			if(strcmp(line, "")==0) break;	//讀到空行
			printf("%s\n", line);
		}
		printf("===分隔線===");
	}
}
```
